# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Environment / secrets
.env
.env.local

# Local runtime data (db, logs, indexes, downloaded models, LoRA adapters)
models/
data/
*.gguf
*.sqlite
*.sqlite3
*.db

# Node / web frontend
node_modules/
src/lca/web/frontend/dist/
src/lca/web/frontend/node_modules/

# Claude Code — personal/local config (never commit; may contain bypass perms)
.claude/settings.local.json

# lca local agent state (checkpoints, conversations, screenshots)
.lca/

# OS
Thumbs.db
.DS_Store

# Unsloth's auto-generated compiled trainer cache (created when QLoRA runs in repo root)
unsloth_compiled_cache/
